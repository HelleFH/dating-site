// InterestButton.js
import React from 'react';

function InterestButton({ label, selected, onClick }) {
  return (
    <button
      type="button"
      className={`profile-form-interest-button ${selected ? 'selected' : ''}`}
      onClick={onClick}
    >
      {label}
    </button>
  );
}

export default InterestButton;
